# Agent Instructions

## Agent skills

### Issue tracker

Issues live in this repo's GitHub Issues (`itsramiel/Software-Engineering`), via the `gh` CLI. See `docs/agents/issue-tracker.md`.

### Domain docs

Single-context: one `CONTEXT.md` + `docs/adr/` at the repo root. See `docs/agents/domain.md`.
